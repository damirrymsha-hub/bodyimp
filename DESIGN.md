# BodyImp — дизайн-система

Минималистичный health-трекер (Telegram Mini App). Светлая тема, много воздуха,
карточки без рамок на мягких тенях, единственный «чернильный» акцент + цветные
маркеры макросов. Этот файл — источник правды при доработке дизайна.

## 1. Токены

### Цвета (tailwind.config.js)
| Токен | HEX | Роль |
|---|---|---|
| `bg` | `#F6F7F9` | фон страницы (холодный серый) |
| `card` | `#FFFFFF` | поверхности карточек |
| `ink` | `#1A1A1A` | основной текст |
| `muted` | `#8A8F98` | вторичный текст, подписи |
| `accent` / `ring` | `#111111` | главный акцент: кнопки, кольцо, активные табы |
| `protein` | `#FF7A59` | белки (коралл) |
| `fat` | `#FFC24B` | жиры (янтарь) |
| `carbs` | `#4F9DDE` | углеводы (голубой) |
| `water` | `#3BA7F0` | вода |
| `steps` | `#7C5CFC` | активность/сожжённые ккал (фиолет) |

Служебные оттенки через прозрачность ink: `bg-ink/5` (подложки, прогресс-треки),
`bg-ink/10` (focus-ring), `border-ink/15` (пунктирные зоны загрузки),
`active:bg-ink/[0.03]` (нажатие строки). Дорожка кольца: `#EDEFF2`.
Опасные действия: красный `text-red-500` на подложке `bg-red-500/10`.
Избранное: жёлтая звезда `text-yellow-400`.

### Типографика
- Шрифт: **Inter** (Google Fonts, weights 400/500/600/700/800) + системный фолбэк.
- Масштаб: заголовок экрана `text-xl font-extrabold` (Onboarding: `text-2xl`);
  большие числа `text-4xl–5xl font-extrabold` (кольцо, итог онбординга);
  число карточки `text-2xl font-extrabold leading-none`;
  заголовок карточки/кнопки `text-sm font-semibold`;
  вторичный текст `text-xs font-medium text-muted`;
  микроподписи `text-[10px]–[11px] text-muted`;
  секции-«брови» `text-xs font-semibold uppercase text-muted`.

### Форма и глубина
- Радиусы: карточки `rounded-3xl` (24px) и `rounded-[2rem]` (32px, крупные секции);
  поля/кнопки `rounded-2xl` (16px); чипы/пилюли `rounded-full`;
  bottom-sheet `rounded-t-[2rem]`.
- Тень: единственная — `shadow-card: 0 2px 12px rgba(0,0,0,0.04)`. Рамок нет
  (исключение: пунктир `border-2 border-dashed border-ink/15` у зон камеры/загрузки
  и `border-2` у выбранных карточек онбординга).
- Отступы: контент экрана `px-5`, низ `pb-28` (под FAB); внутри карточек `p-4/p-5`;
  между секциями `mt-4/mt-6`; списки `gap-2`, формы `gap-3`.

### Движение
- Bottom-sheet: framer-motion, `initial {y:'100%'}` → spring `damping 28, stiffness 300`.
- Онбординг: слайды `opacity+x`, 0.2s. Прогресс-бары/кольцо: CSS transition 0.5–0.6s ease.
- Haptics Telegram на каждый значимый тап (light/medium + success).

## 2. Компоненты

- **Карточка-метрика** (вода, активность): иконка+заголовок слева, кнопка-действие
  справа (круг 28px `bg-{color}/10 text-{color}` с Plus), крупное число со
  вторичной единицей, прогресс-бар `h-2 rounded-full` на `bg-ink/5`.
- **NutritionRing**: SVG-кольцо 168px, дуга `#111` со скруглёнными концами по
  треку `#EDEFF2`, в центре «N ккал осталось».
- **MacroCard**: строка «Белки 62/162 г» + тонкий цветной бар.
- **FoodItem**: белая строка-карточка `rounded-3xl p-4`: название + мета
  («Обед · Б 28 · Ж 12 · У 31»), справа ккал, ⭐ (жёлтая, если в избранном), корзина.
- **Кнопки**: primary — `bg-ink text-white rounded-2xl py-4 text-sm font-semibold`
  на всю ширину; secondary — `bg-ink/5 text-ink`; danger — красный на `bg-red-500/10`;
  disabled — `opacity-40`.
- **Чипы** (приёмы пищи, категории, даты): `rounded-full px-4 py-2 text-sm`;
  активный — `bg-ink text-white`, неактивный — `bg-card text-ink shadow-card`.
- **Сегмент-переключатель** («На порцию | На 100 г»): контейнер `bg-ink/5 rounded-2xl p-1`,
  активный сегмент — белая карточка с тенью.
- **Поля**: класс `.input` — белые, `rounded-2xl px-4 py-3 text-sm`, без рамки,
  `focus:ring-2 ring-ink/10`; лейбл сверху `text-xs text-muted`.
- **Bottom-sheet модалки** (все сценарии добавления): overlay `bg-black/40`,
  панель `max-w-md rounded-t-[2rem] bg-bg p-5 pb-8`, заголовок + круглая
  кнопка-крестик `bg-ink/5`.
- **FAB**: чёрный круг 64px по центру снизу (`bottom-6`), белый Plus 28px.
- **CalendarStrip**: 7 дней, выбранный — чёрный круг с белой цифрой, сегодня —
  `bg-ink/5`.
- **Степпер количества** (AmountModal): [−] поле [+], квадраты 48px `bg-card shadow-card`.
- **Полноэкранные оверлеи** (камера, скан штрихкода): чёрный фон, белые
  контролы-круги `bg-white/15`, рамка-прицел `border-white/70`.
- **Тосты**: сверху, тёмные пилюли.
- **Пустые состояния**: карточка с иконкой 28px и 1–2 строками `text-muted`.

## 3. Экраны

1. **Onboarding** — 5 шагов (пол → данные → активность → цель → итог с BMR/TDEE/ИМТ),
   полосковый прогресс сверху, крупные selection-карточки.
2. **Home** — шапка (дата «Сегодня, 29 мая» + аватар-кнопка), CalendarStrip,
   кольцо калорий, блок макросов, карточки Вода+Активность, чёрная CTA «Мой прогресс»,
   список «История», FAB.
3. **AddFood** (bottom-sheet, 6 режимов) — меню из ModeButton-строк (иконка в
   квадрате `bg-ink/5 rounded-2xl` + заголовок + подпись): Вручную / Фото /
   Описать текстом (ИИ) / Штрихкод / Быстрый поиск / Избранное. Внутри режимов:
   чипы приёма пищи, live-пересчёт «Итого», карточка результата ИИ с правкой.
4. **Profile** — единая форма (пол/возраст/рост/вес/цель/активность),
   read-only карточка «Рассчитанная норма» с live-пересчётом, норма воды.
5. **Progress** — график веса/калорий за неделю (Recharts), лог веса.

## 4. UX-правила
- Телефонный однcolonный layout, максимум `max-w-md`; всё крупное и тапабельное (цели ≥44px).
- Тексты интерфейса — русские, короткие; числа — главный герой экрана.
- Один сценарий = один bottom-sheet; закрытие — тап по overlay или крестик.
- Никаких hover-состояний (тач); обратная связь — haptic + `active:`.
- Светлая тема принципиально (пока); тёмная — только осознанным решением.

---

## 5. Промт для доработки дизайна в Claude

Скопируй блок ниже вместе с разделами 1–4 этого файла:

```
Ты — продуктовый дизайнер. Дорабатываешь дизайн BodyImp — Telegram Mini App
для трекинга питания (светлый минимализм). Ниже — действующая дизайн-система,
она источник правды.

ЧТО СОХРАНИТЬ (не обсуждается):
- светлая тема: фон #F6F7F9, белые карточки БЕЗ рамок на мягкой тени
  (0 2px 12px rgba(0,0,0,.04)), чернильный акцент #111 для кнопок/активных состояний;
- цветовое кодирование: белки #FF7A59, жиры #FFC24B, углеводы #4F9DDE,
  вода #3BA7F0, активность #7C5CFC — цвет только как маркер, дозированно;
- Inter, крупные extrabold-числа как главный визуальный элемент;
- большие радиусы (16/24/32px), bottom-sheet сценарии, чёрный FAB;
- мобильный однокколонный layout под Telegram WebView, тап-цели ≥44px.

ЧЕГО НЕ ДЕЛАТЬ:
- не добавлять градиенты, glassmorphism, тёмную тему, новые шрифты, рамки
  и лишние тени; не красить фоны карточек в цвета; не мельчить текст (< 10px);
- не превращать минимализм в пустоту: интервалы из системы (см. токены),
  а не «ещё больше воздуха».

НА ЧТО ОБРАТИТЬ ВНИМАНИЕ (мои болевые точки):
1) Иерархия Home: кольцо → макросы → вода/активность → история; проверить ритм
   отступов и не перегружен ли экран.
2) Меню AddFood из 6 пунктов стало длинным — возможно, сетка 2×3 вместо списка?
3) Контраст muted-текста #8A8F98 на #F6F7F9 — проверить читаемость мелких подписей.
4) Состояния загрузки/ошибок ИИ-анализа (фото/текст) — сделать их приятнее.
5) Экран Progress самый слабый — предложи редизайн графиков в стиле системы.

Формат ответа: сначала 3–5 конкретных проблем текущего дизайна со скриншотов/
описания, потом варианты решения (mockup в HTML/React + tailwind с моими
токенами), каждый — с обоснованием в одну строку.
```
