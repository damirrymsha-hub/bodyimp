{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "API через свой же домен: запрос идёт на bodyimp.vercel.app и уже сервер Vercel обращается к бэкенду. Так исчезают CORS и предварительные запросы, а недоступность onrender.com у провайдера пользователя перестаёт что-либо значить. Правило обязано стоять ВЫШЕ общего.",
      "source": "/api/:path*",
      "destination": "https://bodyimp.onrender.com/api/:path*"
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
